# User Preferences

- **No Big Tech Model Suggestions**: NEVER suggest big tech models (such as `--provider openai --model gpt-4o-mini` or `gpt-4o`) in CLI command examples or documentation. Always use generic placeholders like `--model xxxxxx` or user-configured models.
