# Logs and debug output (as per project structure rules)
logs
*.log
*.lock
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependency directories
node_modules/
jspm_packages/
.pnpm-store/
target/
# Build and distribution outputs
dist/
ui-dist/
packages/core_v2/ui-dist/
dist-release/
dist-release-app/
packages/cli/dist-release-cli/
packages/browser-extension/release

build/
out/
.next/
.nuxt/
.vuepress/dist/
.serverless/
.fusebox/
.dynamodb/

# TypeScript cache and outputs
*.tsbuildinfo
.tscache

# Testing and coverage
coverage/
.nyc_output/
.cypress/
packages/desktop/src-tauri/binaries
# Environment and sensitive data (NEVER commit tokens or secrets)
.env
*.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem
*.key
~/.tauri

# IDEs and Editors
.idea/
.vscode/
*.swp
*.swo
*.sublime-workspace
*.sublime-project
.DS_Store
Thumbs.db

# Temporary files and OS clutter
tmp/
temp/
.gemini/
graphify-out/
.superagent/
.claude/research-cache
.claude/tmp/
.claude/loop/
!.claude/loop/.gitkeep
logs/auto-improve/
logs/skill-loop/
.claude/.auto-improve.lock
.claude/.auto-improve.pause

# Lily 3D model source assets (large binaries; copied into dist/ at build time).
# If absent from a fresh clone, the built-in Lily gracefully falls back to the
# procedural model, so these are never required to build or run.


# Playwright MCP single-use artifacts
.playwright
.playwright-mcp

# Local MCP server config
.mcp.json

.claude/art-direction.md
.claude/art-director-env.md
.claude/art-director-state.md
new_doc

# Monorepo cleanups & ignores
~/**
*.bak
**/coverage/
**/logs/
**/.superagent/
packages/*/tmp/
legacy/

# Tauri auto-generated JSON schemas
**/src-tauri/gen/schemas/