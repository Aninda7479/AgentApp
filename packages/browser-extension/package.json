{
  "name": "@superagent/browser-extension",
  "version": "0.40.0",
  "description": "SuperAgent Browser Extension — On the way chat and agentic browser automation connected to core_v2 and global memory",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^20.17.10",
    "typescript": "^5.7.2",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
